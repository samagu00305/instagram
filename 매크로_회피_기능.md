# 인스타그램 매크로 회피 기능 문서

## 📋 개요
이 문서는 인스타그램 자동화 프로그램에 구현된 매크로 탐지 회피 기능들을 정리한 문서입니다.

---

## ✅ 구현된 매크로 회피 기능

### 1. 랜덤 딜레이 (Random Delay)
**파일**: `Util.ahk` - `RandomDelay()` 함수
**설정 위치**: `GlobalData.ahk`

#### 기능 설명
- 각 액션 사이에 **불규칙한 대기 시간**을 추가하여 일정한 패턴을 회피
- 기준 시간을 입력하면 자동으로 랜덤 범위 계산

#### 동작 방식
```
입력: RandomDelay(10)
→ 기준 시간: 10초
→ 변동폭: 10 × 0.3 = ±3초
→ 실제 대기: 7~13초 사이 랜덤
```

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_RandomDelayEnabled()` | true | 랜덤 딜레이 활성화 여부 |
| `g_RandomDelayVariationRatio()` | 0.3 (30%) | 변동폭 비율 |
| `g_RandomDelayMinVariation()` | 1초 | 최소 변동폭 |
| `g_RandomDelayMaxVariation()` | 5초 | 최대 변동폭 |

#### 적용 위치
- 홈 화면 클릭 후
- 댓글 버튼 클릭 후
- AI 질문 후 답변 대기
- 좋아요/팔로우 클릭 후
- 모든 주요 액션 사이

---

### 2. 스크롤 패턴 다양화 (Scroll Variation)
**파일**: `Util.ahk` - `RandomScroll()` 함수
**설정 위치**: `GlobalData.ahk`

#### 기능 설명
- 항상 같은 방식으로 스크롤하지 않고 **3가지 패턴을 랜덤**으로 사용

#### 스크롤 패턴
| 패턴 | 확률 | 동작 |
|-----|------|------|
| PageDown 방식 | 50% | PgDn 키를 1~3회 랜덤 실행 |
| 휠 스크롤 방식 | 30% | 마우스 휠을 3~8회 작게 여러 번 |
| 읽는 척 방식 | 20% | 위로 1~3회 → 아래로 2~5회 (읽는 것처럼) |

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_ScrollVariationEnabled()` | true | 스크롤 다양화 활성화 |

#### 적용 위치
- 게시물 탐색 중 스크롤할 때

---

### 3. 랜덤 정지 (Random Pause)
**파일**: `Util.ahk` - `RandomPause()` 함수
**설정 위치**: `GlobalData.ahk`

#### 기능 설명
- 확률적으로 **0.5~2초 동안 정지**
- 사람이 게시물을 확인하는 시간을 시뮬레이션

#### 동작 방식
- 30% 확률로 발생 (설정 가능)
- 0.5~2초 사이 랜덤 정지

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_RandomPauseEnabled()` | true | 랜덤 정지 활성화 |
| `g_RandomActionProbability()` | 0.3 (30%) | 발생 확률 |

#### 적용 위치
- 게시물 확인 전
- 댓글 확인 중

---

### 4. 랜덤 마우스 이동 (Random Mouse Move)
**파일**: `Util.ahk` - `RandomMouseMove()` 함수
**설정 위치**: `GlobalData.ahk`

#### 기능 설명
- 화면 내 **랜덤한 위치로 마우스를 이동**
- 사람처럼 마우스를 움직이는 행동 시뮬레이션

#### 동작 방식
- 30% 확률로 발생
- 화면 전체에서 랜덤 위치로 부드럽게 이동 (속도 50)

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_RandomMouseMoveEnabled()` | true | 랜덤 마우스 이동 활성화 |
| `g_RandomActionProbability()` | 0.3 (30%) | 발생 확률 |

#### 적용 위치
- 좋아요 버튼 클릭 전

---

### 5. 읽는 척하기 (Random Reading)
**파일**: `Util.ahk` - `RandomReading()` 함수
**설정 위치**: `GlobalData.ahk`

#### 기능 설명
- 텍스트 영역에서 마우스를 **아래로 천천히 이동**
- 글을 읽는 것처럼 자연스러운 행동 시뮬레이션

#### 동작 방식
- 30% 확률로 발생
- 2~5회 아래쪽으로 천천히 이동 (속도 30)
- 각 이동마다 0.3초 딜레이

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_RandomMouseMoveEnabled()` | true | 마우스 이동 활성화 |
| `g_RandomActionProbability()` | 0.3 (30%) | 발생 확률 |

#### 적용 위치
- 댓글 내용 확인 중

---

### 6. 랜덤 실수 행동 (Random Mistake)
**파일**: `Util.ahk` - `RandomMistake()` 함수
**설정 위치**: `GlobalData.ahk`

#### 기능 설명
- 가끔 **실수하는 것처럼 행동**
- ESC 누르기, 뒤로가기 등 사람의 실수를 시뮬레이션

#### 실수 패턴
| 패턴 | 확률 | 동작 |
|-----|------|------|
| ESC 키 | 50% | ESC를 누르고 0.5초 대기 |
| 뒤로가기 | 50% | Browser_Back → 1초 대기 → Browser_Forward |

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_RandomMistakeEnabled()` | true | 실수 행동 활성화 |
| `g_RandomMistakeProbability()` | 0.05 (5%) | 발생 확률 |

#### 적용 위치
- 작업 완료 후 (팔로우 완료/댓글 작성 완료)

---

### 7. 시간대별 활동 패턴 (Time-Based Activity Pattern)
**파일**: `Util.ahk` - `IsActiveTimeNow()`, `CheckSessionTimeout()` 함수
**설정 위치**: `GlobalData.ahk` - `GetActivityProbabilityByHour()` 함수

#### 기능 설명
- **직장인 생활 패턴**을 시뮬레이션하여 자연스러운 활동 시간 구현
- 수면 시간대(00:00~06:00)에는 **완전히 동작하지 않음**
- 시간대별로 다른 활동 확률 적용
- 세션 기반 활동: 일정 시간 활동 후 휴식

#### 시간대별 활동 확률
| 시간대 | 활동 확률 | 설명 |
|-------|----------|------|
| 00:00~06:00 | 0% | 수면 시간 - **동작 안 함** |
| 06:00~09:00 | 20% | 기상~출근 시간 |
| 09:00~12:00 | 90% | 오전 업무 시간 - 활발 |
| 12:00~13:00 | 70% | 점심 시간 |
| 13:00~18:00 | 50% | 오후 업무 시간 |
| 18:00~21:00 | 80% | 퇴근 후 - 활발 |
| 21:00~24:00 | 30% | 저녁/취침 전 |

#### 세션 기반 활동
- **한 세션 활동 시간**: 30~60분 (랜덤)
- **세션 사이 휴식**: 15~45분 (랜덤)
- 사람처럼 일정 시간 활동 후 휴식하는 패턴

#### 동작 방식
```
예시 1) 오전 10시 (활동 확률 90%)
→ 90% 확률로 활동 시작
→ 30~60분 활동
→ 15~45분 휴식
→ 반복

예시 2) 새벽 3시 (활동 확률 0%)
→ 다음 활성 시간대(06:00)까지 대기
→ 동작하지 않음
```

#### 설정 파라미터
| 설정 함수 | 기본값 | 설명 |
|---------|-------|------|
| `g_TimeBasedActivityEnabled()` | true | 시간대별 활동 패턴 활성화 |
| `g_SessionMinMinutes()` | 30분 | 한 세션 최소 활동 시간 |
| `g_SessionMaxMinutes()` | 60분 | 한 세션 최대 활동 시간 |
| `g_SessionBreakMinMinutes()` | 15분 | 세션 사이 최소 휴식 시간 |
| `g_SessionBreakMaxMinutes()` | 45분 | 세션 사이 최대 휴식 시간 |

#### 적용 위치
- 메인 루프 시작 전 시간 체크
- 각 작업 사이에 세션 타임아웃 체크

---

## 🎯 설정 방법

### 전체 활성화/비활성화
`GlobalData.ahk` 파일에서 다음 함수를 수정:

```autohotkey
; 전체 랜덤 행동 ON/OFF
g_RandomActionEnabled()
{
    return true  ; false로 변경하면 모든 랜덤 행동 비활성화
}
```

### 개별 기능 활성화/비활성화
`GlobalData.ahk` 파일에서 각 기능별로 ON/OFF 가능:

```autohotkey
; 랜덤 딜레이
g_RandomDelayEnabled() { return true }

; 스크롤 다양화
g_ScrollVariationEnabled() { return true }

; 랜덤 마우스 이동
g_RandomMouseMoveEnabled() { return true }

; 랜덤 정지
g_RandomPauseEnabled() { return true }

; 실수 행동
g_RandomMistakeEnabled() { return true }

; 시간대별 활동 패턴
g_TimeBasedActivityEnabled() { return true }
```

### 확률 조정
```autohotkey
; 랜덤 행동 발생 확률 (0.0 ~ 1.0)
g_RandomActionProbability()
{
    return 0.3  ; 30% → 원하는 값으로 변경
}

; 실수 행동 발생 확률
g_RandomMistakeProbability()
{
    return 0.05  ; 5% → 원하는 값으로 변경
}
```

### 랜덤 딜레이 변동폭 조정
```autohotkey
; 변동폭 비율 (0.3 = ±30%)
g_RandomDelayVariationRatio()
{
    return 0.3  ; 0.5로 변경하면 ±50%
}

; 최소 변동폭
g_RandomDelayMinVariation()
{
    return 1  ; 최소 1초
}

; 최대 변동폭
g_RandomDelayMaxVariation()
{
    return 5  ; 최대 5초
}
```

### 시간대별 활동 패턴 조정
```autohotkey
; 세션 활동 시간 (분 단위)
g_SessionMinMinutes()
{
    return 30  ; 최소 30분
}

g_SessionMaxMinutes()
{
    return 60  ; 최대 60분
}

; 세션 휴식 시간 (분 단위)
g_SessionBreakMinMinutes()
{
    return 15  ; 최소 15분
}

g_SessionBreakMaxMinutes()
{
    return 45  ; 최대 45분
}

; 시간대별 활동 확률 커스터마이징
; GlobalData.ahk의 GetActivityProbabilityByHour() 함수에서 수정
GetActivityProbabilityByHour(hour)
{
    if (hour >= 0 && hour < 6)
        return 0  ; 수면 시간 - 0%로 설정하면 완전히 동작 안 함

    ; 다른 시간대도 원하는 대로 조정 가능
    ; ...
}
```

---

## 📊 매크로 회피 효과

### 구현 전
- 모든 액션이 **정확히 같은 시간** 간격
- 항상 **같은 방식**으로 스크롤
- **기계적인 패턴** 반복

### 구현 후
- 각 액션마다 **±30% 시간 변동**
- **3가지 스크롤 패턴** 랜덤 사용
- **30% 확률**로 정지/마우스 이동
- **5% 확률**로 실수 행동
- **시간대별 활동 패턴** (수면 시간 0%, 직장인 패턴)
- **세션 기반 활동** (30~60분 활동 후 15~45분 휴식)
- **더욱 사람같은 행동 패턴**

---

## ⚠️ 주의사항

1. **과도한 확률 설정 주의**
   - 실수 행동 확률을 너무 높이면 오히려 비효율적
   - 권장: 실수 행동 5% 이하, 일반 행동 30% 정도

2. **완벽한 회피는 불가능**
   - 이 기능들은 매크로 탐지를 **회피**하는 데 도움을 주지만
   - 완벽한 보장은 할 수 없음
   - 인스타그램 정책을 준수하는 것이 중요

3. **시간대별 활동 패턴**
   - 수면 시간(00:00~06:00)에는 완전히 동작하지 않음
   - 비활성화하려면 `g_TimeBasedActivityEnabled()`를 false로 설정
   - 시간대별 확률을 너무 낮게 설정하면 활동량이 부족할 수 있음

4. **디버그 모드**
   - `g_DebugMode()` 활성화 시 모든 랜덤 행동이 로그로 출력됨
   - 테스트 시 유용

---

## 📝 업데이트 이력

### v2.1 (2025-01-15)
- ✅ 시간대별 활동 패턴 추가 (직장인 패턴)
- ✅ 수면 시간(00:00~06:00) 완전 비활성화
- ✅ 세션 기반 활동 시스템 (30~60분 활동, 15~45분 휴식)
- ✅ 문서 업데이트: 시간대별 활동 패턴 섹션 추가

### v2.0 (2025-01-15)
- ✅ 랜덤 딜레이 자동 계산 방식 추가
- ✅ 스크롤 패턴 다양화 (3가지 패턴)
- ✅ 랜덤 정지 기능 추가
- ✅ 랜덤 마우스 이동 추가
- ✅ 읽는 척하기 행동 추가
- ✅ 랜덤 실수 행동 추가 (ESC, 뒤로가기)

### v1.0 (초기 버전)
- 기본 자동화 기능만 구현

---

## 🔗 관련 파일

| 파일 | 역할 |
|-----|------|
| `GlobalData.ahk` | 모든 설정값 정의 |
| `Util.ahk` | 랜덤 행동 함수 구현 |
| `Main.ahk` | 랜덤 행동 적용 |

---

**마지막 업데이트**: 2025-01-15
**작성자**: Claude Code
**버전**: 2.1
